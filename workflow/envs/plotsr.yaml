channels:
  - conda-forge
  - bioconda
channel_priority: strict
dependencies:
  - python>=3.11
  - minimap2
  - samtools
  - syri
  - plotsr
  - matplotlib
  - pillow
  - pandas<2.0  # pandas 2.0+ CoW causes SyRI crash (read-only array in Cython)
